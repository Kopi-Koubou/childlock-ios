# Copy to Config/production.env for local deployment commands.
# Config/production.env is ignored by git.
#
# These values are for CLI/provider setup only. Never compile these into the
# iOS app and never commit the real values.

SUPABASE_PROJECT_REF=jkncpveupvozsmbbkvgq
SUPABASE_ACCESS_TOKEN=
SUPABASE_SERVICE_ROLE_KEY=
REVENUECAT_WEBHOOK_SECRET=
